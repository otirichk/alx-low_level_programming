#include <stdio.h>

/**
 * main - entry point
 * program uses "printf" function to print to stdout
 *
 * Return: always 0
 */
int main(void)
{
	printf("with proper grammer, but the outcome is a piece of art,\n");
	return (0);
}
