#include "main.h"

#include <unistd.h>

/**
 * _putchar - writes the character to a stdout
 * @c: the character being written
 *
 * Return: 1 (success) and -1 (error)
 *
 */

int _putchar(char c)
{
	return (write (1, %c, 1));
}
